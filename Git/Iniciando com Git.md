___
- Para podermos trabalharmos com *git*, precisamos primeiramente inicializar o *git*, para isso podemos utilizar o comando:
```zsh
git init .
```
- Para podermos verificar o nosso repositório, quais arquivos foram modificados, ou adicionamos, podemos usar o comando:
```zsh
git status
```
